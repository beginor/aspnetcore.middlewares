<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <AssemblyName>Beginor.AspNetCore.Middlewares.CustomHeader</AssemblyName>
    <RootNamespace>Beginor.AspNetCore.Middlewares.CustomHeader</RootNamespace>
    <Authors>beginor</Authors>
    <Description>Beginor.AspNetCore.Middlewares.CustomHeader</Description>
    <PackageId>Beginor.AspNetCore.Middlewares.CustomHeader</PackageId>
    <Owners>beginor</Owners>
    <PackageVersion>$(PACKAGE_VERSION)</PackageVersion>
    <PackageReleaseNotes>$(PACKAGE_RELEASE_NOTES)</PackageReleaseNotes>
    <PackageTags>$(PACKAGE_TAGS)</PackageTags>
    <PackageProjectUrl>$(PACKAGE_PROJECT_URL)</PackageProjectUrl>
    <Title>Beginor.AspNetCore.Middlewares.CustomHeader</Title>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

</Project>
